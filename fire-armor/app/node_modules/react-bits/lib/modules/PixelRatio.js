"use strict";
exports.__esModule = true;
var ReactBits_1 = require("../ReactBits");
exports.PixelRatio = {
    get: function () {
        return ReactBits_1.ReactBits.Dimensions.get('window').scale;
    },
    getFontScale: function () {
        return ReactBits_1.ReactBits.Dimensions.get('window').fontScale || exports.PixelRatio.get();
    },
    getPixelSizeForLayoutSize: function (layoutSize) {
        return Math.round(layoutSize * exports.PixelRatio.get());
    },
    roundToNearestPixel: function (layoutSize) {
        var ratio = exports.PixelRatio.get();
        return Math.round(layoutSize * ratio) / ratio;
    }
};
